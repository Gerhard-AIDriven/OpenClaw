# Cloudflare Pages _redirects file
# Documentation: https://developers.cloudflare.com/pages/configuration/serving-pages/#redirects

# IMPORTANT: Cloudflare Pages serves static files automatically
# We only need redirects for special cases

# DO NOT use catch-all rewrites - they break static file serving
# Remove any /* /index.html rules

# If you need custom redirects, add them here:
# /old-path /new-path 301

# That's it! Static files (HTML, CSS, JS, images) are served automatically.
