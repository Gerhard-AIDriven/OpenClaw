# 2026-09-20

## Automated Report Poller — recovered (cron: c70e61e8)

The "Automated Report Generator (every 3 min)" cron job was silently failing (agent turn "ok" but script never ran).

**Root cause:** The repo working tree is checked out on branch `main`, which is 208 commits BEHIND `master` and does NOT contain the whatsapp project files. The entire whatsapp automation (pollers, report engine, LINZ/hazards APIs, gmail notifier, rates scraper) exists only on `master` and was absent from the working tree → `node poll-automated-reports-v2.js` threw MODULE_NOT_FOUND.

**What I did:**
- Restored deleted files from `master` into the working tree (on `main`), without switching branches:
  - `node whatsapp/poll-automated-reports-v2.js` + deps (`push-to-github-pages.js`, `report-engine-v2.js`, `linz-api.js`, `hazards-linz-integration.js`, `gmail-notifier.js`, `.env`)
  - `git checkout master -- whatsapp/`
  - `git checkout master -- napier_rates_scraper.py napier_rates_scraper_easements.patch`

**Verified the poller now works:** it fetched 5 queued requests, generated a full report for "31 Douglas McLean Avenue, Marewa, Napier" (LINZ title HBE2/765, 40 easements, hazards data).

**Two remaining blockers (NOT yet fixed):**
1. **Branch mismatch:** repo is on `main`; the deployment helper (`push-to-github-pages.js`) assumes `master` — it does `git checkout gh-pages` → commit → `git checkout master`. Left unfixed; switching the shared repo branch is a config change I flagged rather than did silently.
2. **Deployment failed:** `git checkout gh-pages` aborted because of uncommitted `memory/heartbeat-state.json` and untracked generated report in `reports/html/`. Report was generated but not published to GitHub Pages.
3. (Minor) `napier_rates_scraper.py` was missing → rates scrape failed; now restored at workspace root.

**TODO / needs human decision:** Decide canonical branch (`main` vs `master`). The automation + 208 commits of whatsapp work live on `master`. Either move the worktree back to `master`, or merge `master`→`main`. Until then the deploy step will keep failing on branch state.
